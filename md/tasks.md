# タスク

- [x] 画像読み込みの改修（HEICがSafari以外で読み込めない問題 → Safari限定の仕様と判明、エラーメッセージ改善で対応済み）
- [x] 画面にバージョンを表示させる、末尾にビルド番号を付与してユニークに（ヘッダーに `v1.0.0 build.2` を表示）
- [ ] 画像認識ロジックにTesseract.jsライブラリを追加
  - [ ] ライブラリモード（Tesseract.js）とAPIモード（Claude API）を切り替えられるようにする
  - [ ] デフォルトはライブラリモード（Tesseract.js）を使用
- [x] スコア入力のテキストボックスを100点刻みで入力できるようにする（入力欄右に`00`固定表示、入力値×100で計算）
- [x] スコア入力の値にmin/maxを設定する(min = -100000, max = 200000)（入力欄レベルではmin=-1000/max=2000、計算時に×100）
- [x] スコア入力欄の表示位置を回転できるようにする（東固定で写真アップロードが東目線のみになっている問題への対応）
  - [x] 利用者が東以外の席でも雀卓の配置と画面表示が一致するようにする
